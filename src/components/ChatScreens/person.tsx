import React from 'react';
import { View, Text, StyleSheet, Button, TouchableOpacity } from 'react-native';
import Uploading from '../uploading';

export default function ChatScreenPerson() {
  return <Uploading/>;
}

const styles = StyleSheet.create({

});